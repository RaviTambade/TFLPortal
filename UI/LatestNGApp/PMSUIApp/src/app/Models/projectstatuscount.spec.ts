import { Projectstatuscount } from './projectstatuscount';

describe('Projectstatuscount', () => {
  it('should create an instance', () => {
    expect(new Projectstatuscount()).toBeTruthy();
  });
});
