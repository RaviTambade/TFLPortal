import { Assignedtaskbymanager } from './assignedtaskbymanager';

describe('Assignedtaskbymanager', () => {
  it('should create an instance', () => {
    expect(new Assignedtaskbymanager()).toBeTruthy();
  });
});
